name: Context info
on: 
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  context:
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        
      - name: Environment 
        run: |
          echo ${{ vars.DEMO_VAR }}
      - name: foo
        run: |
          ls
      - uses: ./.github/actions/shared/action.yml@main
