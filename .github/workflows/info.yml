name: Context info
on: 
  workflow_dispatch
  push:
    branches:
      - main
jobs:
  context:
    environment: testing
    runs-on: ubuntu-latest
    steps:
      - name: Environment 
        run: |
          echo ${{ vars.DEMO_VAR }}
      - uses: ./.github/workflows/shared.yml
